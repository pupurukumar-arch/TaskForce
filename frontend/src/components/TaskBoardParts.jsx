import { Link } from 'react-router-dom'
import { canSubmitForReview } from '../lib/taskAccess'

const statusLabel = (status) => status?.replace('_', ' ') || 'To do'

export function TaskCard({ task, projectId, user, isManager, onStatusChange, onApprove }) {
  const assignee = task.assignedTo?.fullName || task.assignedTo?.username || 'Unassigned'
  const latestAttachment = task.attachments?.[task.attachments.length - 1]

  return <article className="rounded-xl border border-slate-200/80 bg-[#fffdf9] p-4 shadow-sm shadow-slate-200/50 transition hover:border-indigo-200 hover:bg-white hover:shadow-md"><div className="flex items-start justify-between gap-3">{isManager ? <h4 className="min-w-0 text-sm font-semibold leading-5 text-slate-800">{task.title}</h4> : <Link to={`/projects/${projectId}/tasks/${task._id}`} className="min-w-0 text-sm font-semibold leading-5 text-slate-800 transition hover:text-indigo-700">{task.title}</Link>}{task.priority && <span className={`shrink-0 rounded-md px-2 py-1 text-[11px] font-semibold ${task.priority === 'high' ? 'bg-red-50 text-red-700' : task.priority === 'medium' ? 'bg-amber-50 text-amber-700' : 'bg-slate-100 text-slate-600'}`}>{task.priority}</span>}</div><p className="mt-3 text-xs text-slate-500">Assigned to <span className="font-medium text-slate-700">{assignee}</span></p>{isManager && task.status !== 'in_review' && <Link to={`/projects/${projectId}/tasks/${task._id}`} className="mt-3 inline-flex rounded-lg border border-indigo-100 bg-indigo-50 px-3 py-2 text-xs font-semibold text-indigo-700 transition hover:bg-indigo-100">Edit task</Link>}{isManager && task.status === 'in_review' && <div className="mt-3 flex flex-wrap gap-2"><Link to={`/projects/${projectId}/tasks/${task._id}`} className="inline-flex rounded-lg border border-indigo-100 bg-indigo-50 px-3 py-2 text-xs font-semibold text-indigo-700 transition hover:bg-indigo-100">Review task</Link>{latestAttachment && <a href={latestAttachment.url} target="_blank" rel="noreferrer" className="inline-flex rounded-lg bg-amber-100 px-3 py-2 text-xs font-semibold text-amber-900 transition hover:bg-amber-200">Open attachment</a>}</div>}{isManager ? <label className="mt-4 block text-xs font-medium text-slate-500">Move task<select value={task.status} onChange={(event) => onStatusChange(task, event.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 bg-slate-50 px-2 py-2 text-xs font-medium text-slate-700 outline-none focus:border-indigo-400"><option value="todo">To do</option><option value="in_progress">In progress</option><option value="in_review">In review</option><option value="done">Done</option></select></label> : canSubmitForReview(task, user._id) ? <Link to={`/projects/${projectId}/tasks/${task._id}`} className="mt-4 inline-flex rounded-lg border border-amber-200 bg-amber-50 px-3 py-2 text-xs font-semibold text-amber-800 hover:bg-amber-100">Attach work & submit</Link> : null}{isManager && task.status === 'in_review' && <div className="mt-4 flex gap-2"><button onClick={() => onApprove(task, true)} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-semibold text-white hover:bg-emerald-700">Approve</button><button onClick={() => onApprove(task, false)} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-600 hover:bg-slate-50">Send back</button></div>}<div className="mt-4 flex items-center justify-between border-t border-slate-100 pt-3 text-xs text-slate-500"><span className="capitalize">{task.difficulty || 'medium'}</span><span>{task.dueDate ? `Due ${new Date(task.dueDate).toLocaleDateString()}` : 'No due date'}</span></div></article>
}

export function TaskCreateModal({ projectId, form, setForm, members, role, creating, onClose, onSubmit }) {
  return <div className="fixed inset-0 z-20 grid place-items-center bg-slate-950/40 p-5 backdrop-blur-sm"><form onSubmit={onSubmit} className="max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-2xl bg-white p-7 shadow-2xl"><p className="text-sm font-medium text-indigo-600">Project work</p><h3 className="mt-1 text-2xl font-semibold text-slate-900">Create a task</h3><label className="mt-6 block text-sm font-medium text-slate-700">Task title<input required value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 outline-none focus:border-indigo-400 focus:ring-4 focus:ring-indigo-100" /></label><label className="mt-4 block text-sm font-medium text-slate-700">Description <span className="font-normal text-slate-400">(optional)</span><textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} className="mt-2 min-h-24 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 outline-none focus:border-indigo-400 focus:ring-4 focus:ring-indigo-100" /></label><label className="mt-4 block text-sm font-medium text-slate-700">Assign to<select value={form.assignedTo} onChange={(event) => setForm({ ...form, assignedTo: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3">{members.map((member) => <option key={member.user._id} value={member.user._id}>{member.user.fullName || member.user.username} ({statusLabel(member.role)})</option>)}</select></label>{role === 'admin' && <Link to={`/projects/${projectId}/members`} className="mt-2 inline-block text-xs font-semibold text-indigo-700 hover:text-indigo-900">+ Add a project member</Link>}<div className="mt-4 grid gap-3 sm:grid-cols-3"><label className="text-sm font-medium text-slate-700">Priority<select value={form.priority} onChange={(event) => setForm({ ...form, priority: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3"><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option></select></label><label className="text-sm font-medium text-slate-700">Difficulty<select value={form.difficulty} onChange={(event) => setForm({ ...form, difficulty: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3"><option value="easy">Easy</option><option value="medium">Medium</option><option value="hard">Hard</option></select></label><label className="text-sm font-medium text-slate-700">Due date<input type="date" value={form.dueDate} onChange={(event) => setForm({ ...form, dueDate: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3" /></label></div><div className="mt-7 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-xl px-4 py-2 text-sm font-medium text-slate-600 hover:bg-slate-100">Cancel</button><button disabled={creating} className="rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{creating ? 'Creating…' : 'Create task'}</button></div></form></div>
}
